alias rm='rm -i'
alias mv='mv -i'
alias cp='cp -i'
alias e="emacsclient -n"
alias ls="ls -p"

alias httpserver='python -m SimpleHTTPServer'

if [ -f ~/.custom_bash_aliases ]; then
    . ~/.custom_bash_aliases
fi
